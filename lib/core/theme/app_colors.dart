import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  // Primary and secondary are intentionally swapped based on request.
  static const Color primary = Color(0xFF003B5C);
  static const Color secondary = Color(0xFF00ADEF);
  static const Color tertiary = Color(0xFFE78D1B);
  static const Color neutral = Color(0xFFF5F7F8);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color text = Color(0xFF1F2933);
}
